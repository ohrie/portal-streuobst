# Allow all crawlers by default
User-agent: *
Allow: /

# /impressum/ und /datenschutz/ werden bewusst NICHT per Disallow gesperrt:
# sie tragen ein <meta name="robots" content="noindex"> und müssen dafür
# crawlbar bleiben. Ein Disallow würde das Auslesen des Tags verhindern.
